# crossword-chat pack (local)

CrosswordChat's **residue** pack: the repo's own instances of rules the mounted canon states in
general. Declared by hand as `local/crossword-chat` in `.claudinite-settings.json`.

The judgment lives in canon: `host-page` owns guesting in a web app you do not own, `web-speech`
owns the browser voice surfaces, and `chrome-extension` owns how the code reaches the page. What
is here is what no other repo could use: the `xwd__` token the arch test enforces and the
`page-adapter/` wall it guards, nytimes.com's idle auto-pause and the bare-`Shift` nudge, the
pencil button's unreadable ON state, the fixture and its `localhost:8787` dev build, the
`selectors.js` verification record, and the REQ and MT ids that carry the evidence.

**No checks.** The `xwd__` quarantine scan is the project's own test
(`extension-test/unit/arch.test.js`).

**Adding to it:** a rule that reads as true of any repo belongs in canon, not here.
